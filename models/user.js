// Inicializamos un arreglo vacío para almacenar los usuarios en memoria
const users = [];

// Exportamos el arreglo de usuarios para que pueda ser utilizado en otros archivos
module.exports = { users };
